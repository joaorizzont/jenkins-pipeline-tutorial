pipeline {
    agent {
        label 'maven'
    }

    stages {
        stage('Steps') {
            steps {
            // One or more steps need to be included within the steps block.
            }
        }

        stage('test') {
            steps {
            // One or more steps need to be included within the steps block.
            }
        }
    }
}
